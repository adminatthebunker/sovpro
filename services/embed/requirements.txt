# CPU-only torch — huge size saving vs. CUDA build.
torch==2.5.1+cpu
# FlagEmbedding pins its own transformers / sentence-transformers versions;
# let pip resolve the transitive stack rather than fighting it.
FlagEmbedding==1.3.2
# HTTP layer
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
