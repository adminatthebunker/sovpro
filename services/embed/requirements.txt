# torch is inherited from the pytorch/pytorch base image (2.5.1 + CUDA 12.4).
# Pinning here would force pip to reinstall it, losing the CUDA build.
#
# FlagEmbedding pins its own transformers / sentence-transformers / accelerate
# versions; we let pip resolve the transitive stack.
FlagEmbedding==1.3.2
# HTTP layer
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
