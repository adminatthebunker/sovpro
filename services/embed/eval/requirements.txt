# Eval-only dependencies. Kept separate from services/embed/requirements.txt so
# the production inference image stays lean.

# Model loading for Qwen3 (HuggingFace path).
transformers>=4.46,<5.0
sentence-transformers>=3.0,<4.0
torch>=2.5,<3.0

# Retrieval scoring (IR metrics).
ir_measures>=0.3.6

# DB access (mirrors services/scanner).
asyncpg>=0.30
httpx>=0.27
orjson>=3.10

# Stratified sampling + small data munging.
numpy>=1.26
